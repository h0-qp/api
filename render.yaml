services:
  - type: web
    runtime: python
    name: pytele-loader
    repo: https://github.com/h0-qp/api
    branch: main
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 api.py
    autoDeploy: false
    envVars:
      - key: REPO_URL
        sync: false
      - key: REPO_BRANCH
        sync: false
      - key: START_CMD
        sync: false
      - key: PORT
        value: 80
